# Template file for 'neko-kernel-manager'
pkgname=neko-kernel-manager
version=1.1.0
revision=1
build_style=meson
hostmakedepends="pkg-config qt6-tools qt6-declarative cargo meson"
makedepends="qt6-base-devel qt6-declarative-devel fmt-devel"
short_desc="Modern kernel manager for Void Linux"
maintainer="ErzaGOD19 <gamingofdemon19@gmail.com>"
license="GPL-2.0-only"
homepage="https://github.com/ErzaGOD19/Neko-Kernel-Manager"
distfiles="${homepage}/archive/refs/tags/v${version}.tar.gz"
checksum=REPLACE_WITH_SHA256_AFTER_RELEASE

# Note: This project uses Cargo via Meson. To build in a restricted environment
# like xbps-src's chroot, you may need to vendor Rust dependencies or 
# allow network access during the build phase (not recommended for official repos).
pre_build() {
	export CARGO_HOME="${wrksrc}/.cargo"
}
